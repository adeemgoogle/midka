func (a *E) unmarshall(resp []byte) ([]EH, error) {
	data := new(Data)

	err := json.Unmarshal(resp, &data)
	if err != nil {
		return nil, err
	}

	if err != nil {
		return nil, err
	}

	return data.BeforeTransfData, nil
}
"data": [
            {
                "createdAt": 1678711541000,
                "amount": -10000,
                "type": "manually",
                "description": "Сгорание",
                "imgUrl": "https://p.evrika.com/logo.svg"
            },}

type EH struct {
	CreatedAt   int    `json:"createdAt,omitempty"`
	Amount      int    `json:"amount,omitempty"`
	Type        string `json:"type,omitempty"`
	Description string `json:"description,omitempty"`
}

type Data struct {
	MerchantId       string              `json:"merchantId"`
	BeforeTransfData []EvrikahistoryResp `json:"data"`
}

error":"json: cannot unmarshal object into Go struct field DataEvrika.
data of type []EH"}
