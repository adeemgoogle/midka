using Microsoft.AspNetCore.Mvc;
using Microsoft.Extensions.Logging;
using Moq;
using NUnit.Framework;
using TradePointAPI;


namespace TradePointAPI.Tests
{
    public class HealthzControllerTests 
    {
        [Test]
        public void Test_Healthz(){
            var loggerMock = new Mock<ILogger<HealthzController>>();
            var controller = new HealthzController(loggerMock.Object);
            var result = controller.GetHealthz() as OkObjectResult;
            
            Assert.IsNotNull(result);
            Assert.AreEqual(200, result.StatusCode);
            Assert.AreEqual("OK", result.Value);
        }
    }
}

Не удалось найти тип или имя пространства имен "HealthzController" (возможно, отсутствует директива using или ссылка на сборку)
