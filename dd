 public string ReadTrades()
 {
     if (isReadingTable["BOOK"])
     {
         return "Already reading BOOK table";
     }
     checkConnection();

     isReadingTable["BOOK"] = true;
     Thread thread = new Thread(WriteTrades);
     thread.Start();

     return "OK";
 }

write Xunit test for this function
