unit.Sdk.IsTypeException
Assert.IsType() Failure
Expected: Microsoft.AspNetCore.Mvc.OkObjectResult
Actual:   (null)
 public class QueryControllerControllerTests
    {
        private readonly ITestOutputHelper _testOutputHelper;
        [Fact]
        public async Task Post_WithValidQuery_ReturnsOkResult()
        {
            // Arrange
            var schemaMock = new Mock<ISchema>();
            var documentExecuterMock = new Mock<IDocumentExecuter>();

            var controller = new QueryController(schemaMock.Object, documentExecuterMock.Object);

            var query = new QueryDtoTest
            {
                query = "ID : "555454545"
            }

var result = await controller.Post(query) as OkObjectResult;
            

            //Console.WriteLine("RESULT HERE" + result);
            _testOutputHelper.WriteLine("Result here", result.Value);
           Assert.IsType<OkObjectResult>(result);
           Debug.Assert(result != null, nameof(result) + " != null");
           Assert.Equal(200, result.StatusCode);
}

fix it 
